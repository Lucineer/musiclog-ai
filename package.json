{
  "name": "musiclog-ai",
  "version": "1.0.0",
  "description": "Repo-native AI agent for musicians — practice tracking, theory explanations, repertoire management",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
